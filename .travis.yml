dist: xenial
language: cpp

compiler:
  - g++

before_install:
    # - sudo apt-get update
    - sudo apt-get install libopencv-dev

install:
    - mkdir build
    - cd build
    - cmake ..
    - make


script:
    - cd ../merlict
    - ../build/merlict-test
